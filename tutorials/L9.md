This assignment uses some new files. The main file (that you run) is cmc_modules2.py, it uses dm_productions2.py, which uses retrieve.py and chunk_noise.py. Chunk utility noise can be adjusted in chunk_noise.py and threshold can be adjusted in retrieve.py. Right now, these are set to retrieve putine a bit more than fries (i.e., the agent sometimes confuses fries with poutine), and sometimes fails to retrieve anything (i.e., the agent can't remember). Adjust these two variables to make a different pattern of retrieval and put a comment at the top of your file explaining how they changed
